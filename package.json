{
  "name": "typescript-users-and-posts-v2",
  "version": "1.0.0",
  "description": "TypeScript exercise with reusable code and an Express server",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "node build/usersAndPosts.js",
    "server": "node build/server/index.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  }
}
